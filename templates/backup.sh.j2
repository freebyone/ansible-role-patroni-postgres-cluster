#!/bin/bash

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="{{ postgresql_backup_dir }}"

mkdir -p $BACKUP_DIR

pg_dumpall -h localhost -U postgres -f $BACKUP_DIR/full_backup_$DATE.sql

find $BACKUP_DIR -name "*.sql" -mtime +7 -delete

echo "Backup completed: $BACKUP_DIR/full_backup_$DATE.sql"